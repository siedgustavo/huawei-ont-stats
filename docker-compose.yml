services:
  router-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_IP=192.168.18.1
      - API_USER=Epadmin
      - API_PASS=adminEp
    restart: unless-stopped
    command: python -m routerstats.api_server --host=0.0.0.0 --port=8000 --router-host=192.168.18.1 --user=Epadmin --pass=adminEp